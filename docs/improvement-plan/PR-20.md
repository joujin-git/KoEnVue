# PR-20: XML/Codec/Path 19 케이스 단위 테스트 + 가시성 완화

**Status**: ✅ completed (3-commit 박음 + docs 동기화 + 박제 정정 — main HEAD `51ba261`, 2026-05-28)
**Branch**: main (PR-15 이후 KoEnVue 는 PR 단위 main 직접 작업 패턴)
**Base**: main (HEAD = 7e32f23, PR-18 5-commit + design doc + docs + wip session 직후)
**Risk**: Low (tests/ 디렉토리만 신규, IL 영향 0)
**Estimated session size**: M (1.5-2h, 새 세션 권장 — PR-20 단독)
**Depends on**: PR-10 (xUnit + InternalsVisibleTo 인프라), PR-15 (BuildStartupTaskXml RunLevel 분기 박제), PR-03 (SanitizeLogPath B1 보안 표면)
**Blocks**: v0.9.5.0 릴리스 (5 PR 계획 #5/5)

## 1. 목적 (Why)

세션 #1 [17:00] 블록의 5 PR 계획 마지막 코드 PR. 세션 #4 (PR-18) 의 P4 예외 축소 + ARM64 volatility 회복까지 코드 변경 모두 종결 — 본 PR 는 **회귀 차단 가치** 측면의 마무리.

세 함수의 공통 성격:

- **XML / 문자열 변환 / 경로 검증** — 입출력이 명확하고 외부 의존성 0 ↔ 단위 테스트가 가장 잘 들어맞는 영역
- **회귀 위험 = High** — `BuildStartupTaskXml` 는 PR-03 D 회귀 (LogonTrigger.UserId 누락) 의 발생지, `SanitizeLogPath` 는 B1 보안 표면 (admin 토큰 가정이 사라진 후 잔여 sanitize), `XmlEntityCodec` 는 PR-15 의 `RunLevel + UserId escape` 가 본 코덱에 의존
- **현재 회귀 0 매치** — 본 PR 는 **회귀 발견 후 fix 가 아니라 "현재 동작이 정확한지 박제" 목적**. v0.9.5.0 이후 누군가 (사용자 / Claude 자신) 가 이 함수를 건드릴 때 silent 회귀 차단

박제 가정의 5 PR 계획 (`#5 = PR-20 19 케이스 + InternalsVisibleTo`) 그대로. 박제 정정 2건:

- InternalsVisibleTo 는 **이미** 존재 ([KoEnVue.csproj:44](../../KoEnVue.csproj#L44)) — 새로 추가 안 함
- `BuildStartupTaskXml` 는 **private** — 가시성 완화 1글자 필요

## 2. 변경 범위 (What)

### 2.1 수정 — App/Startup/StartupTaskManager.cs (~1 LOC delta + remarks 3줄)

[StartupTaskManager.cs:163](../../App/Startup/StartupTaskManager.cs#L163):

```csharp
private static string BuildStartupTaskXml(string exePath, bool adminElevation)
```

↓

```csharp
internal static string BuildStartupTaskXml(string exePath, bool adminElevation)
```

근거: 본 메서드는 schtasks XML 의 단일 진실원 — PR-03 D 회귀 (LogonTrigger.UserId) 재발 차단을 위해 단위 테스트가 필요. `internal` 로 완화하면 `InternalsVisibleTo("KoEnVue.Tests")` (PR-10) 로 접근 가능. 본 모듈은 이미 internal class 이므로 일관성도 OK. 외부 (App 다른 모듈, Core) 노출 0.

### 2.2 신규 — tests/KoEnVue.Tests/Unit/StartupTaskXmlTests.cs (~80 LOC, 6 케이스)

**위치**: `tests/KoEnVue.Tests/Unit/StartupTaskXmlTests.cs` 신규
**책임**: `BuildStartupTaskXml(exePath, adminElevation)` 가 schtasks 2.0 XML 의 6 invariant 를 박제

6 케이스 매트릭스:

| # | 케이스 | 입력 | 검증 | 가드 근거 |
|---|--------|------|------|----------|
| 1 | `RunLevel_AdminTrue_EmitsHighestAvailable` | `(@"C:\KoEnVue.exe", true)` | XML.Contains(`<RunLevel>HighestAvailable</RunLevel>`) | PR-15 admin_elevation true 시 schtasks `/RL HIGHEST` 동치 |
| 2 | `RunLevel_AdminFalse_EmitsLeastPrivilege` | `(@"C:\KoEnVue.exe", false)` | XML.Contains(`<RunLevel>LeastPrivilege</RunLevel>`) | PR-03 asInvoker 디폴트 (admin 토큰 0) |
| 3 | `Delay_AlwaysEmitsPT15S` | `(@"C:\KoEnVue.exe", false)` | XML.Contains(`<Delay>PT15S</Delay>`) | StartupTaskDelay = "PT15S" — explorer 트레이 초기화 race 회피 |
| 4 | `LogonTrigger_UserId_Present_AndEscaped` | `(@"C:\KoEnVue.exe", false)` | XML.Contains(`<UserId>`) && `</UserId>` && `\` 포함 && 비어있지 않음 (사용자 결정 #1 권장: CI portable) | PR-03 D 회귀 차단 (UserId 누락 → ANY-user trigger → admin 요구) |
| 5 | `Command_Path_WrappedInQuotesAndEscaped` | `(@"C:\Program Files\KoEnVue.exe", false)` | XML.Contains(`<Command>&quot;C:\Program Files\KoEnVue.exe&quot;</Command>`) — Trim('"') 호환 위해 큰따옴표 유지 (XmlEntityCodec.Escape 가 `"` → `&quot;` 변환하므로 XML 안에서는 `&quot;` 형태) | PR-04 분리 시 PathsEqual 마이그레이션 호환 — Command 양끝 큰따옴표 유지 박제 |
| 6 | `Command_Path_WithAmpersand_Escaped` | `(@"C:\foo&bar\KoEnVue.exe", false)` | XML.Contains(`<Command>&quot;C:\foo&amp;bar\KoEnVue.exe&quot;</Command>`) | `XmlEntityCodec.Escape` 호출 박제 (`&` → `&amp;` + `"` → `&quot;` 양쪽 변환) |

### 2.3 신규 — tests/KoEnVue.Tests/Unit/XmlEntityCodecTests.cs (~60 LOC, 5 케이스)

**위치**: `tests/KoEnVue.Tests/Unit/XmlEntityCodecTests.cs` 신규
**책임**: `XmlEntityCodec.Escape/Unescape` 의 5 entity 처리 + round-trip + 중복 방지 박제

5 케이스 매트릭스:

| # | 케이스 | 입력 | 기대 | 가드 근거 |
|---|--------|------|------|----------|
| 1 | `Escape_All5Entities_Replaced` | `"&<>\"'"` | `"&amp;&lt;&gt;&quot;&apos;"` | XML 1.0 spec 5 predefined entities. 한 번에 검증 |
| 2 | `Unescape_All5Entities_Restored` | `"&amp;&lt;&gt;&quot;&apos;"` | `"&<>\"'"` | round-trip 반대 방향 |
| 3 | `RoundTrip_ArbitraryString_Preserves` | `[Theory]` 다중 입력 — 일반 텍스트, 특수문자만, 빈 문자열, 한글, `&&&&&` | `Unescape(Escape(s)) == s` | round-trip invariant |
| 4 | `Escape_AmpersandFirst_NotDoubleEncoded` | `"a&b<c"` | `"a&amp;b&lt;c"` (not `"a&amp;b&amp;lt;c"`) | `&` 가 가장 먼저 처리되어 다른 엔티티의 `&` 가 중복 인코딩 안 됨 |
| 5 | `Unescape_AmpAmpLast_NotMangled` | `"&amp;amp;"` | `"&amp;"` (not `"&"`) | `&amp;` 가 마지막에 처리되어 원본 `&` 가 다른 엔티티의 앰퍼샌드를 의도치 않게 잡아채는 것을 차단 |

### 2.4 신규 — tests/KoEnVue.Tests/Unit/SanitizeLogPathTests.cs (~110 LOC, 8 케이스)

**위치**: `tests/KoEnVue.Tests/Unit/SanitizeLogPathTests.cs` 신규
**책임**: `PortablePath.SanitizeLogPath(requested, out rejectionReason)` 의 PR-03 B1 보안 표면 박제

8 케이스 매트릭스 (4 거부 + 4 허용/폴백):

| # | 케이스 | 입력 | 기대 반환 | 기대 rejectionReason | 가드 근거 |
|---|--------|------|-----------|---------------------|----------|
| 1 | `Reject_System32_OutsideAllowedRoot` | `@"C:\Windows\System32\koenvue.log"` | `ResolveLogPath()` (fallback) | `Contains("outside allowed roots")` | 사용자가 시스템 폴더 지정 시 거부 |
| 2 | `Reject_ParentTraversal_OutsideAllowedRoot` | `Path.Combine(BaseDirectory, @"..\..\..\evil.log")` (정규화 후 BaseDirectory 외) | `ResolveLogPath()` | `Contains("outside allowed roots")` (사용자 결정 #2 권장: `IsUnderAllowedRoot` 환경 의존성 흡수) | `Path.GetFullPath` 정규화 후 BaseDirectory 하위가 아니면 거부 |
| 3 | `Reject_UncPath_OutsideAllowedRoot` | `@"\\server\share\koenvue.log"` | `ResolveLogPath()` | `Contains("outside allowed roots")` | UNC 도 정규화 가능하지만 BaseDirectory/FallbackRoot 하위 아님 |
| 4 | `Reject_OtherUserProfile_OutsideAllowedRoot` | `@"C:\Users\OtherUser\koenvue.log"` | `ResolveLogPath()` | `Contains("outside allowed roots")` | 다른 사용자 프로필로의 write 표면 차단 |
| 5 | `Allow_UnderBaseDirectory` | `Path.Combine(PortablePath.BaseDirectory, "custom.log")` | 입력 그대로 (정규화된 형태) | `null` | BaseDirectory 하위는 허용 |
| 6 | `Allow_UnderFallbackRoot` | `Path.Combine(PortablePath.FallbackRoot, "custom.log")` | 입력 그대로 | `null` | `%LOCALAPPDATA%\KoEnVue` 하위 허용 |
| 7 | `Fallback_NullOrEmpty_ReturnsResolveLogPath` | `[Theory]` `null`, `""`, `"   "` | `ResolveLogPath()` | `null` | 미설정 시 디폴트 경로 폴백 — reject 가 아니므로 reason null |
| 8 | `Fallback_InvalidCharacters_NormalizationFails` | `"C:\\test\0invalid.log"` (NUL 문자 — `|<>*` 는 .NET 8+ 에서 throw 안 할 수 있음) | `ResolveLogPath()` | `Contains("could not be normalized")` | `Path.GetFullPath` ArgumentException → fallback + reason 영문 명시 |

### 2.5 수정 — App/Startup/StartupTaskManager.cs 헤더 코멘트 (~3 LOC remarks)

가시성 완화 정당화를 코멘트로 박제:

```csharp
/// <remarks>
/// PR-20: 본 메서드는 PR-03 D 회귀 (LogonTrigger.UserId 누락) + PR-15 RunLevel 분기의 단일 진실원이라
/// 단위 테스트 (<c>StartupTaskXmlTests</c>) 박제 대상. private → internal 완화는 InternalsVisibleTo
/// ("KoEnVue.Tests") 만으로 노출되며 App/Core 외부에는 노출 0.
/// </remarks>
```

### 2.6 문서

- [ ] [CHANGELOG.md](../../CHANGELOG.md) [Unreleased] / Added — "PR-20 19 케이스 단위 테스트 (StartupTaskXml 6 + XmlEntityCodec 5 + SanitizeLogPath 8) — 회귀 차단" 1행
- [ ] [docs/architecture.md](../architecture.md) tests/ 트리 갱신 — Unit/ 디렉토리 6 행 (기존 3 + 신규 3)
- [ ] [docs/conventions.md](../conventions.md) — P1 dev-only 예외 절에 PR-20 박제 ("XML/codec/path 19 케이스 = 회귀 차단 baseline")
- [ ] `docs/dev-notes/2026-05-28-pr-20-unit-tests.md` 신규 — 가시성 완화 결정 근거 (PR-03 D 회귀 재발 차단 가치) + CI portable invariant 5 (xUnit `[Theory]` 활용 패턴) + 환경 의존성 흡수 패턴
- [ ] [docs/improvement-plan/INDEX.md](INDEX.md) — PR-20 항목 추가

## 3. 비범위 (Out of Scope)

- **`StartupTaskManager` 의 나머지 internal 메서드** (`ToggleStartupRegistration`, `SyncStartupPathCore`, `QueryRegisteredTask`, `ExtractTagFromXml`, `PathsEqual`, `RunSchtasks`) 는 schtasks.exe 실 호출에 의존 → unit 테스트 부적합. integration 테스트는 별도 PR.
- **`PortablePath` 의 `ResolveConfigPath` / `ResolveLogPath`** 는 `IsBaseDirectoryWritable` 캐시 + file probe 의존 → 환경 의존성 큼. 본 PR 는 `SanitizeLogPath` 만 박제.
- **`XmlEntityCodec` 의 numeric character reference** (`&#34;` 등) — XML 1.0 spec 외, 본 코덱은 5 predefined entities 만 처리. 사용 패턴 100% 그러함.
- **PR-15 design doc §5 의 `IsEnvFlagSet` / `GetCurrentIntegrityLevel` 테스트** (`AdminElevation` 모듈) — 박제 가정 [17:00] 블록은 본 4 케이스를 PR-20 에 포함시키지 않음. 후속 PR 후보.

## 4. 3-commit 분할안 (사용자 결정 #3 권장 채택)

각 commit 은 **단일 의미 단위** + **빌드 통과** + **test 늘어남에 따라 점진 PASS**.

| # | commit message | 변경 파일 | LOC delta | 의미 |
|---|----------------|----------|-----------|------|
| 1 | `refactor(startup): BuildStartupTaskXml 가시성 private→internal + StartupTaskXmlTests 6 케이스 (PR-20 1/3)` | StartupTaskManager.cs `private→internal` + 헤더 remarks, tests/Unit/StartupTaskXmlTests.cs 신규 (~80) | +84 / -1 | 가장 회귀 위험 높은 함수 먼저 박제. 가시성 변경과 테스트 같이 commit — 검증 시점 박제 |
| 2 | `test(core): XmlEntityCodec 5 케이스 round-trip + 순서 invariant (PR-20 2/3)` | tests/Unit/XmlEntityCodecTests.cs 신규 (~60) | +60 | 가장 단순한 함수. internal → InternalsVisibleTo 로 접근 (이미 PR-10) |
| 3 | `test(config): SanitizeLogPath 8 케이스 B1 보안 표면 회귀 가드 (PR-20 3/3)` | tests/Unit/SanitizeLogPathTests.cs 신규 (~110) | +110 | public 메서드 — 가시성 변경 없음. 환경 의존성 흡수 패턴 박제 |

**커밋 순서 정당화**:

- commit 1 먼저: 가시성 변경 1글자가 핵심 결정 — 별 commit 으로 명시해 reviewer 가 명확히 검토 가능
- commit 2-3 순서는 자유 — internal vs public 차이만 있음. Codec → Path 는 의존성 그래프 순 (Core → App)

## 5. 검증 기준 (Done When)

### 5.1 Tier 1 — 빌드 + 사이즈 + 테스트

- [ ] `dotnet build` — 0 warn / 0 error (commit 단위로 검증)
- [ ] `dotnet publish -r win-x64 -c Release` — 0 warn / 0 error
- [ ] **AOT 사이즈 가드**: baseline `4,861,440 bytes` (PR-18 [19:39] 박제) → **±0 bytes 예상** (tests/ 디렉토리는 publish 입력 아님 + `private→internal` 가시성은 IL 사이즈 무영향). +1 KB 초과 시 일시 정지
- [ ] `dotnet test tests/KoEnVue.Tests/` — **40 → 65 PASS** (기존 40 + 신규 25 PASS). 0 fail / 0 skip. 박제 가정 "19 케이스 → 19 PASS" 정정: 19 = **[Fact] + [Theory] 메서드 단위** (StartupTaskXml 6 + XmlEntityCodec 5 + SanitizeLogPath 8 매트릭스), 25 = **xUnit 이 InlineData 를 개별 test 로 카운트** (Theory 의 InlineData 분리 효과). §9 박제 정정표 참조
- [ ] invariant 4종 (CLAUDE.md) — 모두 0 매치

### 5.2 Tier 2 — invariant grep (신규 가드 + 기존 유지)

```bash
# 신규 테스트 파일 namespace 가드
git grep "namespace KoEnVue.Tests.Unit" tests/KoEnVue.Tests/Unit/StartupTaskXmlTests.cs    # 1 매치
git grep "namespace KoEnVue.Tests.Unit" tests/KoEnVue.Tests/Unit/XmlEntityCodecTests.cs    # 1 매치
git grep "namespace KoEnVue.Tests.Unit" tests/KoEnVue.Tests/Unit/SanitizeLogPathTests.cs   # 1 매치

# 가시성 완화 가드 — internal 한 곳만
git grep -n "internal static string BuildStartupTaskXml" App/Startup/StartupTaskManager.cs    # 1 매치
git grep -n "private static string BuildStartupTaskXml" App/Startup/StartupTaskManager.cs     # 0 매치

# InternalsVisibleTo 유지 가드 (PR-10 이후)
git grep "InternalsVisibleTo" KoEnVue.csproj    # 1 매치 (KoEnVue.Tests)

# 케이스 수 가드 — [Fact] + [Theory] 합산이 기대값
git grep -cE "(\[Fact\]|\[Theory\])" tests/KoEnVue.Tests/Unit/StartupTaskXmlTests.cs       # 5 매치 (Theory 1 + Fact 4)
git grep -cE "(\[Fact\]|\[Theory\])" tests/KoEnVue.Tests/Unit/XmlEntityCodecTests.cs       # 5 매치 (Theory 1 + Fact 4)
git grep -cE "(\[Fact\]|\[Theory\])" tests/KoEnVue.Tests/Unit/SanitizeLogPathTests.cs      # 4 매치 (Theory 2 + Fact 4)

# P6 (App → Core 단방향) — 신규 테스트가 Core 만 의존 (XmlEntityCodec)
git grep -n "using KoEnVue.Core" tests/KoEnVue.Tests/Unit/XmlEntityCodecTests.cs    # 1 매치
```

### 5.3 Tier 3 — 사용자 수동 smoke (사용자 결정 #4 권장: 선택적 5분)

본 PR 는 tests/ 디렉토리만 신규 (publish IL 영향 0) + 가시성 1글자 변경 (IL 영향 0) 이라 **사용자 가시 smoke 불필요**. release exe 의 동작 변화 0.

선택적 검증 (5분):

- [ ] `bin\Release\net10.0-windows\win-x64\publish\KoEnVue.exe` 실행 → 기존 동작 동일 (트레이 표시 + 한/En/EN 인디 + 자동 시작 토글 정상)
- [ ] config.json 의 `log_file_path` 를 `C:\Windows\System32\koenvue.log` 로 변경 후 재시작 → koenvue.log 가 fallback 경로 (`%LOCALAPPDATA%\KoEnVue\koenvue.log`) 에 생성 + Warning "outside allowed roots" 기록

## 6. 사이드 이펙트 / 위험

### 위험 1: `BuildStartupTaskXml` 가시성 완화 후 외부 호출 누출

`private → internal` 후 같은 assembly 의 다른 모듈이 직접 호출하면 캡슐화 깨짐. 본 메서드는 `RegisterStartupTaskWithXml` 에서만 호출되어야 함.

**완화**: §5.2 grep 가드에 `BuildStartupTaskXml` 호출처 검증:

```bash
git grep -n "BuildStartupTaskXml" App/Startup/StartupTaskManager.cs    # 2 매치 (정의 L163 + 호출)
git grep -n "BuildStartupTaskXml" App/                                  # 2 매치 (위와 동일)
git grep -n "BuildStartupTaskXml" Core/                                  # 0 매치 (P6 호환)
git grep -n "BuildStartupTaskXml" tests/                                 # ≥1 매치 (StartupTaskXmlTests 안)
```

### 위험 2: 테스트 환경 의존성

- `StartupTaskXmlTests` case 4 (LogonTrigger UserId): `Environment.UserDomainName` 의 ASCII 가정. **완화 (결정 #1 권장)**: 비어있지 않음 + `\` 포함만 박제.
- `SanitizeLogPathTests` case 2 (ParentTraversal): `BaseDirectory` 깊이 의존. **완화 (결정 #2 권장)**: `IsUnderAllowedRoot` 로 환경 흡수.
- `SanitizeLogPathTests` case 8 (InvalidChars): Windows-only invalid chars 사용. `tests/KoEnVue.Tests.csproj` `TargetFramework = net10.0-windows` 가 Windows-only 보장.

### 위험 3: AOT 사이즈 미증 보장

가시성 `private → internal` 은 C# 컴파일러 출력 IL 동일. `InternalsVisibleTo` 도 metadata 만 영향. **예상 사이즈 delta = ±0 bytes**.

### 위험 4: `XmlEntityCodec` 의 implementation detail 박제

케이스 4-5 (순서 invariant) 가 `.Replace()` chain 순서를 직접 박제. 미래 fluent API 전환 시 정정 부담 가능.

**완화**: 테스트가 **결과만** 검증 — `Escape("a&b<c") == "a&amp;b&lt;c"` 가 정확하면 구현 무관. 코멘트로 "결과 invariant" 명시.

### 위험 5: P4 / P6 invariant 위반 잠재

tests/ 는 App/Core 양쪽 다 import 가능 (P6 는 release exe 만 적용 — tests/ 는 dev-only 예외). 본 PR 의 테스트는 명시적 `using KoEnVue.Core.Xml` / `using KoEnVue.App.Startup` / `using KoEnVue.App.Config` 만 사용해 P6 정신 보존.

## 7. 롤백 절차

- 3 commit 의 각 commit 은 **독립 revert 가능**. dependency 0
- 가시성 변경 (commit 1) revert 시 테스트 빌드 실패 → commit 1 의 테스트도 같이 revert
- 한 테스트 케이스 실패 시 → 그 케이스만 정정 또는 `[Theory]` InlineData 제거
- 전 회귀 시 → 3 commit 전부 revert. 코드 + IL 영향 0 이라 부작용 0
- 데이터 영향 없음 — config.json / log 형식 변경 0

## 8. 사용자 승인 결정 사항 (사용자 4종 권장 채택 확정)

| # | 결정 | 채택 | 근거 |
|---|------|------|------|
| 1 | `LogonTrigger.UserId` 케이스 4 강도 | **"비어있지 않음" + `\` 포함만 박제** (CI portable) | CI runner 호스트명 ASCII 의존 회피. PR-03 D 회귀의 핵심은 "UserId 누락" 이라 비어있지 않음 박제로 충분 |
| 2 | `SanitizeLogPath` ParentTraversal 처리 | **`IsUnderAllowedRoot` 환경 의존성 흡수** | 환경 흡수가 더 robust. 절대 경로는 시스템 폴더 거부 (case 1) 와 의미 중복 |
| 3 | commit 분할 | **3-commit** (가시성+1 / 2 / 3 테스트) | 가시성 변경과 검증 테스트가 같이 commit 되는 게 검증 시점 명확. 2-3 commit 은 단순 add |
| 4 | Tier-3 사용자 smoke | **선택적 5분** (log_file_path 시스템 폴더 거부 evidence) | 본 PR Low risk. SanitizeLogPath 사용자 가시 evidence 5분은 가치 있지만 필수 아님 |

## 9. 박제 가정 정정 (PR-18 패턴)

세션 #1 [17:00] 블록 박제 가정 vs 실제:

| 박제 가정 | 실제 | 정정 |
|---|---|---|
| "InternalsVisibleTo 1줄 (KoEnVue.csproj)" 신규 추가 | **PR-10 (de98e7b, 2026-05-21) 때 이미 추가** | 가정 **틀림** — 신규 추가 0. commit 1 의 1줄 작업은 `BuildStartupTaskXml` **가시성 완화** (`private → internal`) |
| "총: 5 LOC + 3 파일" | 실제 = 수정 1 (가시성 1글자 + remarks 3줄) + 신규 3 + 문서 5 | "5 LOC" 의 5 = 가시성 + remarks 합산 정합 |
| "BuildStartupTaskXml 6 케이스" | PR-15 design doc §5 = **2 케이스** (admin true/false). 메인 세션 [17:00] 박제 = **6 케이스** 확장 | 박제 = 메인 세션이 회귀 차단 가치로 PR-15 의 2 케이스를 6 케이스로 확장 — 정본 |
| "XmlEntityCodec 5 케이스" | 메인 세션 [17:00] 박제 정본. PR-15 design doc 에는 박제 없음 | 본 PR 가 신규 테스트 영역 도입 |
| "SanitizeLogPath 8 케이스" | PR-03 §B1 = 1 케이스, dev-note §74 = 1 케이스. 메인 세션 박제 = **8 케이스 (4 거부 + 4 허용)** 확장 | 박제 = 메인 세션이 PR-03 의 1 케이스를 8 케이스로 확장 — 정본 |
| "trailing 거부" | `SanitizeLogPath` 코드에 trailing 분기 없음 — trailing 슬래시는 `Path.GetFullPath` 정규화에서 자동 처리 | 박제 "trailing" → **4 거부 = System32 / ParentTraversal / UNC / OtherUserProfile** 로 정본화 |
| "test 40 → 59 PASS (19 신규)" | 실제 = **40 → 65 PASS (25 신규)**. 19 = `[Fact] + [Theory]` **메서드 단위** (StartupTaskXml 6 + XmlEntityCodec 5 + SanitizeLogPath 8), 25 = xUnit 이 **`[Theory]` 의 InlineData 를 개별 test 로 카운트** 한 PASS 수 | "19 케이스" 어휘는 매트릭스 메서드 카운트로 유지, "19 PASS" 만 25 로 정정 — 본 PR §5.1 도 같이 갱신 |
| §2.2 case 5/6 검증식 `"..."` → `&quot;...&quot;` | 박제 가정은 raw `"` 표시이나 실제 `XmlEntityCodec.Escape` 가 `"` → `&quot;` 변환 — XML 결과 안에서는 `&quot;` 형태 | 박제 가정 cosmetic 정정 — 테스트 코드 [StartupTaskXmlTests.cs:50/58](../../tests/KoEnVue.Tests/Unit/StartupTaskXmlTests.cs) 는 이미 `&quot;` 형태로 정확 |
| §2.4 case 8 InvalidChars = `\|<>*` | `\|<>*` 는 .NET 8+ 에서 `Path.GetFullPath` ArgumentException throw 안 할 수 있음 (런타임 정책 완화) — throw 가 확실한 **NUL 문자 (`\0`)** 로 변경 | 박제 가정 정정 — 테스트 코드 [SanitizeLogPathTests.cs:78](../../tests/KoEnVue.Tests/Unit/SanitizeLogPathTests.cs) 는 이미 NUL 사용 |

## 10. 세션 진행 로그

### 2026-05-28 — planner 산출 (본 design doc)

- `App/Startup/StartupTaskManager.cs` 직접 read — `BuildStartupTaskXml` private 확인 → 가시성 완화 1글자 필요 발견
- `Core/Xml/XmlEntityCodec.cs` 직접 read — `internal static class` 확인 → InternalsVisibleTo 로 접근 가능
- `App/Config/PortablePath.cs` 직접 read — `SanitizeLogPath` public 확인 + `out string? rejectionReason` 영문 메시지 시그니처 확인
- `tests/KoEnVue.Tests/Unit/SettingsValidateTests.cs` + `ColorHelperTests.cs` + `DpiHelperTests.cs` 직접 read — xUnit `[Fact]` / `[Theory]` 컨벤션 + `AppConfig() with { ... }` record-with 패턴 추출
- `KoEnVue.csproj:44` 의 `<InternalsVisibleTo Include="KoEnVue.Tests" />` 발견 → 박제 "신규 추가" 가정 정정
- PR-15 design doc §5 직접 read — `BuildStartupTaskXml` 2 케이스 박제 확인 → 메인 세션 [17:00] 의 6 케이스로 확장 정당화
- PR-03 design doc §B1 + dev-note (asinvoker-migration) §74 직접 read — `SanitizeLogPath` 1 케이스 박제 확인 → 메인 세션 [17:00] 의 8 케이스 (4 거부 + 4 허용) 매트릭스 정본화
- 3-commit 분할 + 4 사용자 결정사항 권장 옵션 + Tier-3 선택적 smoke 5분 매트릭스 작성

### 다음 단계 (구현 진입)

사용자 본 design doc 초안 승인 + §8 4 결정 권장 채택 → commit 1 부터 순차 박음.

각 commit 후:

1. `dotnet build` + `dotnet test tests/KoEnVue.Tests/` (40 → 46 → 55 → 65 점진 증가 박제 — InlineData 분리 카운트 정정 후) + AOT publish
2. verifier sub-agent 위임 (invariant + 사이즈 가드)
3. 3 commit 완료 후 reviewer sub-agent (전체 diff + P1-P6 + 가시성 완화 정당화 + 환경 의존성 흡수 패턴)

### 2026-05-28 — 3-commit 구현 + docs 동기화 (본 세션)

commit 분할 박음 (모두 push):

- `24fdeae` (1/3) — `BuildStartupTaskXml` 가시성 `private → internal` + remarks 3줄 + [StartupTaskXmlTests.cs](../../tests/KoEnVue.Tests/Unit/StartupTaskXmlTests.cs) 6 PASS (Theory 1 메서드 = InlineData 2 PASS 분리)
- `55ba31e` (2/3) — [XmlEntityCodecTests.cs](../../tests/KoEnVue.Tests/Unit/XmlEntityCodecTests.cs) 9 PASS (Theory 1 메서드 = InlineData 5 PASS 분리)
- `51ba261` (3/3) — [SanitizeLogPathTests.cs](../../tests/KoEnVue.Tests/Unit/SanitizeLogPathTests.cs) 10 PASS (Theory 2 메서드 = InlineData 6 PASS 분리)

게이트 결과:

- `dotnet build` (Debug) + `dotnet publish -r win-x64 -c Release` (AOT) 양쪽 0 warn / 0 error
- AOT publish 사이즈: **4,861,440 bytes** (PR-18 [19:39] baseline 동일, ±0 — 가시성 변경 IL 영향 0 + tests/ 는 publish 입력 아님)
- 테스트: **40 → 65 PASS** (신규 25). 0 fail / 0 skip
- publish exe SHA256: `8484f89ba6e25b62e995ac31713c16e6391542061c86cd045ff568f0a0f252a4`
- P1-P6 invariant 22항목 (13 기본 + 9 PR-20 신규) 모두 PASS

박제 정정 (본 세션 발견):

- §2.2 case 5/6 검증식 raw `"` → XML entity 형태 `&quot;` (XmlEntityCodec.Escape 가 `"` 변환 — 테스트 코드는 이미 정확)
- §2.4 case 8 InvalidChars `|<>*` → NUL 문자 (`\0`) — `|<>*` 가 .NET 8+ 에서 throw 안 할 수 있어 NUL 로 변경 (테스트 코드는 이미 NUL 사용)
- §5.1 test 카운트 "40 → 59 PASS (19 신규)" → "40 → 65 PASS (25 신규)" — Theory InlineData 가 xUnit 에서 개별 test 로 카운트되는 효과 정정

docs 동기화 (docs-keeper 산출):

- [CHANGELOG.md](../../CHANGELOG.md) Unreleased / Added — PR-20 한 줄 박제
- [docs/architecture.md](../architecture.md) tests/ 트리 갱신 — Unit/ 6 파일 (기존 3 + 신규 3)
- [docs/conventions.md](../conventions.md) P1 dev-only 예외 + conventions.md L68 grep stale 정정 (`ShellExecuteW.*runas` → `RunAsVerb` const 추출 후 doc comment 만 매치하는 문제)
- [docs/dev-notes/2026-05-28-pr-20-unit-tests.md](../dev-notes/2026-05-28-pr-20-unit-tests.md) 신규
- [docs/improvement-plan/INDEX.md](INDEX.md) PR-20 항목 추가

후속 후보 (reviewer 식별):

1. PR-15 §5 AdminElevation 4 케이스 (`IsEnvFlagSet` + `GetCurrentIntegrityLevel`)
2. `SyncStartupPathCore` integration 테스트 (schtasks mock 필요 — 보류 권고)
4. (옵션) 사용자 Tier-3 5분 smoke (`log_file_path` 시스템 폴더 거부 evidence)
5. docs-keeper (CHANGELOG / architecture / conventions / dev-notes / improvement-plan/INDEX) → main FF merge
