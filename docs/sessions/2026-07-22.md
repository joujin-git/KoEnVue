
## [17:35] turn

**최근 응답 발췌:**

> 완료했습니다. 커밋 `ecdc89b` 푸시됨, working tree clean.  ## 무엇이 깨져 있었나  `docs/harness.md:391` 이 적어둔 전제 — *"C: 드라이브는 보안 정책상 수시로 14일 전 시점으로 복원"* — 이 6주 공백 동안 실제로 발생했고, **그에 대비해 만든 복구 hook 이 정확히 그 순간에 무동작**했습니다.  ''' C: 프로젝트 디렉토리 통째로 재생성(오늘 16:08) → memory 서브디렉토리 자체가 부재   → Get-AutoMemoryDir 이 Test-Path 실패로 $null 반환     → Sync-Memory 첫 줄에서 즉시 return  ← 복구 불발       → 알림 조건이 absorbed/restored > 0 이라 침묵 '''  E: 쪽…

**문서 동기화 대기:**
- 17:26:14|.claude/hooks/lib/_common.ps1|docs/harness.md
- 17:26:26|.claude/hooks/lib/_common.ps1|docs/harness.md
- 17:26:29|.claude/hooks/lib/_common.ps1|docs/harness.md
- 17:26:37|.claude/hooks/session-start.ps1|docs/harness.md
- 17:27:19|.claude/settings.json|docs/harness.md
- 17:32:31|.claude/memory/safety-net-verify-in-failure-state.md|docs/harness.md
- 17:32:38|.claude/memory/MEMORY.md|docs/harness.md
- 17:33:28|.claude/settings.json|docs/harness.md


## [18:15] turn

**최근 응답 발췌:**

> 수치까지 확인됐습니다. 워크플로우 결과를 기다리는 동안 **제가 코드 정독으로 확정한 것**을 먼저 보고드립니다.  ## 결론 — 예, 있습니다. 그리고 잠깐 깜빡이는 게 아니라 **계속 떠 있습니다.**  ### 재현 절차  1. 아무 앱(예: 메모장)을 쓰는 중 — 인디케이터 표시됨 2. 트레이 메뉴 → **"메인 인디케이터 숨김" 체크** (또는 트레이 좌클릭) → 인디 사라짐 3. **바탕화면을 클릭** (또는 작업표시줄 클릭) 4. 트레이 메뉴 → **"메인 인디케이터 숨김" 체크 해제** 5. → **인디케이터가 바탕화면 위에 나타나고, 그대로 남아 있습니다**  ### 왜 그런가 — 두 개의 결함이 겹칩니다  **① 숨김 해제가 필터를 아예 안 봅니다.** [Program.cs:1035](Pro…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:14] turn

**최근 응답 발췌:**

> **사용자님이 이미 재현하셨습니다.** 로그에 그 순간이 그대로 남아 있고, 심지어 **완벽한 대조 실험**이 됐습니다.  ## 로그가 보여주는 두 시퀀스 — 차이가 결함 조건을 정확히 짚습니다  **① 19:10:19 — 정상 동작 (자기치유)** ''' 19:10:17.146  Tray toggle: UserHidden=True 19:10:17.147  HideOverlay: source=UserHidden toggle 19:10:19.217  Tray toggle: UserHidden=False          ← 해제 19:10:22.985  Filter triggered HIDE: fgClass=Shell_TrayWnd, streak=3   ← 3.8초 뒤 정상 숨김 ✅ ''' 이때는 해제 시점에…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:20] turn

**최근 응답 발췌:**

> 앱이 DEBUG 로 실행 중입니다(PID 42644). 로그가 `Filter HIDE deferred (streak=N/3)`, `PositionUpdated`, `IME state` 까지 다 찍히니 이제 경로를 정밀 추적할 수 있습니다.  ## 테스트 절차 — 직접 해주셔야 하는 부분입니다  각 묶음 사이에 **10초쯤 쉬어주세요**(로그 구간이 분리돼서 분석이 정확해집니다).  ### A. 핵심 결함 재현  1. 메모장 등 **아무 일반 창을 클릭** → 인디 표시되는지 확인 2. **작업표시줄 빈 곳 클릭** → 인디가 사라지는지 확인 (약 0.2초 뒤) 3. **3초쯤 기다리기** ← 중요. 필터가 "확정" 상태로 잠기는 시간입니다 4. 트레이 아이콘 **좌클릭** (숨김 ON) → 화면 변화 없고…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:25] turn

**최근 응답 발췌:**

> 로그가 **완벽한 대조 실험**이 됐습니다. 결함 2회 재현 + 정상 1회가 모두 잡혔습니다.  ## 결론 — 재현 확정, 조건까지 정확히 특정됐습니다  | # | 숨김 해제 | 해제 시점 필터 상태 | 이후 숨김 신호 | 인디 표시 지속 | |---|---|---|---|---| | ① 좌클릭 | 19:20:43.785 | **확정됨**(19:20:25 `streak=3`) | ❌ **없음** | **28.6초** | | ② 메뉴 | 19:21:24.523 | **확정됨**(19:21:12 `streak=3`) | ❌ **없음** | **21.5초** | | ③ 좌클릭 | 19:21:51.017 | **미확정**(`streak=2/3`) | ✅ 정상 HIDE | 정상 |  **결함 조건이 정확히 갈립니다*…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:27] turn

**최근 응답 발췌:**

> 알겠습니다. 여기서 멈추고 기다리겠습니다.  현재 상태만 남겨둡니다:  - **앱**: `D:\_portable\KoEnVue\KoEnVue.exe` 실행 중 (PID 42644), `log_level=DEBUG` 유지 - **원본 백업**: config 은 스크래치패드에 `config.json.orig`, 기존 로그는 같은 폴더 `koenvue-before-debug-test.log` - **코드**: 변경 0 — 리포는 clean, `ecdc89b` 에서 그대로입니다  원복이든 재테스트든 수정이든, 말씀하시는 대로 하겠습니다.

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:29] turn

**최근 응답 발췌:**

> 문서에 명확한 답이 있습니다. 사용자님 기억의 출처도 찾았는데, **뜻이 조금 다릅니다.**  ## 사용자님이 기억하시는 건 아마 이것  [docs/KoEnVue_PRD.md:354](docs/KoEnVue_PRD.md:354):  > 필터 평가를 매 폴링으로 단순화한 이유: **"바탕화면 클릭 → 같은 앱 복귀"** 시 `hwndForeground` 는 동일하지만 인디케이터는 **다시 나타나야 한다**. `lastFiltered` 플래그로 중복 메시지는 억제  이건 **"바탕화면에서도 직전 앱에 표시한다"가 아니라** — "바탕화면을 클릭하면 숨기고, **원래 앱으로 돌아오면** 다시 보여준다"는 뜻입니다. 바탕화면을 클릭했다가 같은 앱으로 돌아왔을 때 창 핸들이 안 바뀌어서 인디가 안 돌아오는 문제를 막…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:36] turn

**최근 응답 발췌:**

> 로그에 **검색 창 테스트가 6회** 남아 있습니다. 결론부터 말씀드리면 **정상 동작이고, 설계된 예외 처리가 실제로 작동했습니다.**  ## 답: 시작 메뉴 / 검색 상자에서는 인디가 **표시됩니다** (의도된 설계)  바탕화면·작업표시줄과 정반대로, 이 둘은 **숨김 대상이 아니라 전용 예외 처리** 대상입니다. 한글 검색어를 입력하는 곳이니 당연합니다.  | 근거 | 내용 | |---|---| | [DefaultConfig.cs:153](App/Config/DefaultConfig.cs:153) | `SystemInputProcesses` = `StartMenuExperienceHost`, `SearchHost`, `SearchApp` | | [Program.cs:1422](Program.cs:142…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:39] turn

**최근 응답 발췌:**

> **찾았습니다 — 19:33:14~19:33:50 구간이고, 지금까지 중 가장 완벽한 재현입니다.** 그리고 이게 사용자님이 **맨 처음 물으신 "바탕화면에 인디"의 정체**입니다.  ## 결함 체인 — 세 가지가 겹쳤습니다  | 시각 | 로그 | 내부에서 일어난 일 | |---|---|---| | 19:33:14.729 | `PositionUpdated: process=SearchHost, pos=(1291,450)` | 검색창에 인디 표시. `_currentProcessName="SearchHost"`, **패널 프레임이 캐시에 저장** | | 19:33:29.479 | `Filter HIDE deferred (streak=1/3, Shell_TrayWnd)` | 트레이 클릭 → 셸이 foreground…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:48] turn

**최근 응답 발췌:**

> 로그가 더 쌓였고 **앞선 질문의 답까지 나왔습니다.**  ## 먼저 — 시작 메뉴도 확인됐습니다  ''' 19:43:05.402  PositionUpdated: process=SearchHost,               pos=(1291,450) 19:43:18.927  PositionUpdated: process=StartMenuExperienceHost,  pos=(1291,450)   ← 시작 메뉴! '''  **시작 메뉴에서도 인디가 표시됩니다.** 그리고 두 좌표가 **정확히 같은 것**이 중요합니다 — PRD:74 가 적어둔 *"시작 메뉴는 DWM 프레임이 화면 전체를 덮어 패널 위치를 반영하지 못하므로 **직전 SearchHost 의 패널 rect 를 재사용**한다"* 는 보정 로직이 실제로…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:51] turn

**최근 응답 발췌:**

> 제 `hide_in_fullscreen` 가설이 틀렸습니다. 그러면 분석이 달라지고, **로그에 결정적 사실이 하나 있습니다.**  ## 인디는 사라진 적이 없습니다 — 로그가 증명합니다  19:24:18~19:24:57 왕복 구간 전체에 **`HideOverlay called` 로그가 단 한 줄도 없습니다.** 이 로그는 INFO 레벨이라 인디가 숨겨지면 **반드시** 남습니다(다른 구간에서는 예외 없이 찍혔습니다).  즉 그 40초 동안 인디는 **계속 표시 상태**였고, **위치만 860px 왔다갔다** 했습니다.  **"사라짐"의 정체는 순간이동입니다.** 보고 계시던 자리(x=2511)에서 인디가 없어지고 멀리 떨어진 x=3371 에 다시 그려지니, 그쪽을 안 보고 계셨다면 사라진 것으로 보입니다.…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [19:54] turn

**최근 응답 발췌:**

> **깜빡임의 결정적 증거를 잡았습니다.** 19:51:59~19:52:03 구간입니다.  ## 인디 위치가 0.75초 간격으로 진동합니다  ''' 19:51:59.003  pos=(3371,1334) 19:51:59.937  pos=(2511,1334)   ← 0.934초 19:52:00.782  pos=(3371,1334)   ← 0.845초 19:52:01.529  pos=(2511,1334)   ← 0.747초 19:52:02.276  pos=(3371,1334)   ← 0.747초 19:52:03.019  pos=(2511,1334)   ← 0.743초 '''  **4초 동안 6번 위치 변경, 간격이 0.75초로 수렴합니다.** 이건 F3 를 열고 닫는 조작으로는 절대 설명이 안 됩니다 — **자동으…

**커밋되지 않은 변경:**
```
?? docs/sessions/2026-07-22.md
```


## [20:02] 세션 정리 — 하네스 메모리 복구 + 메인 인디 UserHidden 결함 규명(코드 변경 0)

### 했음

**1부 — 하네스 메모리 split-brain 재발 복구 (커밋 `ecdc89b`)**
- `/resume-session` 으로 시작. 마지막 세션이 2026-06-09 라 **6주 공백**. 코드는 `v0.9.9.6` 이후 무변경(태그 이후 26커밋 전부 `.claude/` + docs), CHANGELOG `[Unreleased]` 비어 있음.
- 상태 점검 중 **C: auto-memory 가 통째로 소실**된 것을 발견 — 프로젝트 디렉토리가 2026-07-22 16:08 에 새로 생성돼 `memory` 서브디렉토리 자체가 부재. `docs/harness.md:391` 이 예견한 "C: 14일 복원" 시나리오가 실제 발생.
- **근본 원인**: `Get-AutoMemoryDir` 이 `Test-Path` 실패 시 `$null` 을 반환하고 `Sync-Memory` 가 즉시 `return` → **복구가 가장 필요한 순간(완전 소실)에 정확히 무동작**. E: 쪽은 없으면 `New-Item` 으로 만들면서 C: 쪽만 포기하는 비대칭. 06-08 검증(`restored=5`)은 디렉토리가 *살아있는* 부분 복원 상태여서 이 경로를 못 봄. 그 결과 **06-09 이후 6주간 메모리 recall 0건**이었고, 알림 조건이 `absorbed/restored>0` 이라 **침묵 실패**였다.
- 수정 3점: ① `Get-AutoMemoryDir` 부재 시에도 원형 slug 경로 반환(대소문자 변형 우선 탐색 — Claude Code 가 07월부터 `e--`→`E--` 로 변경) ② `Sync-Memory` 가 C: 부재 시 생성 후 미러하고 `created` 반환 ③ `session-start.ps1` 이 `created` 시 "C: 복원/초기화 감지" 경고.
- **동종 침묵 실패 1건 추가 발견·수정**: `auto-push` hook 의 matcher 가 `Bash` 뿐인데 이 프로젝트는 `CLAUDE_CODE_USE_POWERSHELL_TOOL=1` 이라 주 셸 도구가 PowerShell → **PowerShell 커밋 시 hook 미발화**. `auto-push.ps1` 은 `tool_input.command` 를 읽어 두 도구 모두 동작하므로 matcher 만 `Bash|PowerShell` 로 확장.
- `autoMemoryDirectory` 를 절대경로(`E:/dev/KoEnVue/.claude/memory`)로 교체 — AUDIT-2 #51 처리. 효력은 다음 세션의 memory 경로 안내로 검증(무효여도 hook 이 보전 → 실패 모드 안전).
- 검증: `created=True`/`restored=7`/`absorbed=0`, E:↔C: **7/7 SHA256 MATCH**. **end-to-end 실증** — C: memory 를 옮겨 부재 재현 → `session-start.ps1` 실행 → 경고 라인이 `additionalContext` 에 주입됨 확인, 프로브 흔적 0 원복. 이 커밋이 PowerShell 도구로 나가면서 auto-push 수정도 실전 검증됨.
- 메모리 `safety-net-verify-in-failure-state` 신설(기존 `tool-limit-verify-first` 와 다른 축 — "된다고 확인한 것도 실패 상태에서 재확인").

**2부 — 메인 인디케이터 UserHidden 해제 결함 규명 (코드 변경 0, [PR-26](../improvement-plan/PR-26-user-hidden-unhide-filter-bypass.md) 박제)**
- 사용자 제보: "메인 인디케이터 숨김 활성화 → 비활성화 시 바탕화면에 인디가 나타나는 케이스가 있는가".
- 코드 정독으로 가설 수립 → **워크플로우 `user-hidden-unhide-hunt`**(4렌즈 병렬 + 적대적 반증, **26 에이전트 / 실패 0 / 확정 18건 · 기각 4건 / 4렌즈 전부 CONFIRMED**) → **DEBUG 로그로 실측 재현 6회**.
- **근본 원인 3중**: ① `ApplyUserHiddenTransition`(`Program.cs:1035`) 표시 분기가 SystemFilter 미재평가 ② `TryHandleFilter`(`:1378`) 의 `!state.LastFiltered && _indicatorVisible` 에지 트리거 — 인디가 꺼진 채 필터 확정되면 HIDE 미전송 + `LastFiltered=true` 래치 → **재숨김 영구 봉인** ③ `_currentProcessName`/`Overlay._lastValidSystemInputFrame` stale + 캐시 무효화 부재 → **닫힌 시작메뉴/검색 패널 좌표(화면 중앙)에 그림** = 최초 제보 "바탕화면에 인디"의 정체.
- **결함 조건 특정**: 해제 시점에 필터가 이미 확정(`streak>=3`)이었는가. 미확정(`streak<3`)이면 자기치유 — 로그에 대조군 다수.
- **결정적 증거는 "없는 로그"**: 확정 이후 구간은 `Filter HIDE deferred` 조차 안 찍혀 완전 침묵(`streak<3` 일 때만 기록되므로). 19:33 시퀀스에서 2.6초 침묵 → 해제 → 18.2초 침묵이 코드 예측과 일치.
- 사양 대조로 **결함 확정**: PRD `:81` 바탕화면=숨김 조건, PRD `:354` `lastFiltered` 역할은 "중복 메시지 억제"뿐, PR-25 `:89/:124/:129` 는 셸 위 잔류를 **명백한 회귀**로 규정하고 dim 유지(후보 c) 미채택.
- **시작 메뉴/검색은 반대로 "표시"가 사양**임도 실측 확인(`SystemInputProcesses`, PRD `:74`). `SearchHost`·`StartMenuExperienceHost` 가 동일 좌표 `(1291,450)` 를 쓴 것으로 "직전 패널 rect 재사용" 보정 작동 확인.
- **미해결 별건**: 토탈 커맨더 인디 위치가 `(2511,1334)↔(3371,1334)` 를 **0.75초 주기로 자동 왕복**(4초간 6회, `HideOverlay` 0건 = 순수 위치 이동). 같은 프로세스의 두 top-level 창이 번갈아 foreground 를 잡는 것으로 추정되나, `PositionUpdated` 로그가 프로세스명만 남겨 **창 특정 불가**. `TLister`(F3 뷰어) 필터 1회 관측(전체화면 아님 확인 → 조건 2/3 추정).

**환경 원복**
- `D:\_portable\KoEnVue\config.json` 의 `log_level` DEBUG→INFO 복원(hot-reload `Config reloaded` 확인). 사용자가 세션 중 켠 `cursor_indicator_enabled=true` 는 **유지**(백업 통째 복원 안 함).
- DEBUG 세션 로그는 스크래치패드에 `koenvue-debug-session-2026-07-22.log` 로 보관. 원래 로그는 `D:\_portable\KoEnVue\koenvue-before-debug-test.log`(335KB)로 남아 있음 — 앱 실행 중이라 파일명 복원은 미수행.

### 왜
- 메모리: hook 이 약속한 복구가 실제 시나리오에서 불발했고 **실패가 조용했다**. 06-09 세션은 같은 상태를 보고도 "C: 가 비었으니 E: 가 활성 store" 라고 정반대로 해석 — 그 오판을 `harness.md §12` 에 명시적으로 정정 기록.
- 인디: 사용자가 **"기록만, 작업은 하지 마"** 로 명시 — 수정 방향(에지 트리거 제거 / 해제 시 필터 재평가 / stale 캐시 무효화)은 후보만 남기고 미착수. 이 영역은 06-08 PR-25 에서 애니메이션 경합 회귀를 겪은 곳이라 설계 선행이 필요.

### 다음
- **PR-26 수정 여부·방향 결정** (사용자 검토 중). 착수 시 planner 설계 → 구현 → verifier 빌드 → reviewer P1–P6 → 실기 smoke 순.
- **토탈 커맨더 진동**: `Program.cs:581` 의 `PositionUpdated` 디버그 로그에 `hwnd`·`GetClassName` 추가 후 재현하면 즉시 판별 가능. 다수 top-level 창 앱 전반의 진단에도 유용.
- **다음 세션 SessionStart 확인 2건**: ① 메모리 동기화 알림(복구 이후라 `restored=0` 조용한 게 정상) ② **`autoMemoryDirectory` 절대경로 효력** — 시스템 컨텍스트의 memory 경로가 `E:\dev\KoEnVue\.claude\memory` 로 바뀌면 split-brain 소멸, 여전히 C: 면 무효(단 hook 이 보전).

### 커밋
- `ecdc89b` fix(harness): Sync-Memory 완전 소실 복구 불발 + auto-push matcher 누락 수정
- (이번 문서 커밋) PR-26 조사 기록 + 세션 정리

---

## [20:05] turn

**최근 응답 발췌:**

> 완료했습니다. 커밋 `fb79699` 푸시됨, working tree clean, **코드 변경 0**입니다.  ## 기록한 것  **[PR-26](docs/improvement-plan/PR-26-user-hidden-unhide-filter-bypass.md)** — 조사 결과를 전부 박제했습니다. 나중에 이 문서만 열면 맥락 없이도 판단하실 수 있게 썼습니다:  - 근본 원인 **3중 구조** (해제 경로 필터 미평가 / `LastFiltered` 래치 / stale 좌표) - 실측 재현 6회 + 대조군 — **결함 조건**(해제 시점 `streak>=3` 여부)까지 특정 - 워크플로우 교차검증 결과(26 에이전트, 4렌즈 CONFIRMED)와 **반증 근거** - PRD·PR-25 대조로 "결함이 맞…


## [20:21] turn

**최근 응답 발췌:**

> 세 가지 모두 처리했습니다.  ## 1. 로그 파일 — 삭제 확인  `D:\_portable\KoEnVue\` 에 `config.json` 과 `KoEnVue.exe` 만 남아 있습니다. `koenvue_crash.txt` 까지 정리되셨네요(그건 크래시 로그가 아니라 앱 시작 시마다 한 줄씩 쌓이던 elevation 진단 파일이라 삭제해도 무해합니다).  ## 2. 디버그 로그에 hwnd + 클래스명 추가 — 완료·배포됨  **바뀐 로그 형태** ''' PositionUpdated: process=TOTALCMD64, hwnd=0x30254, class=TTOTAL_CMD, pos=(2511,1334), saved=0                                      ^^^^^^^^^^^^^…

**문서 동기화 대기:**
- 20:16:19|Core/Logging/Logger.cs|docs/architecture.md
- 20:16:42|Program.cs|docs/architecture.md,docs/implementation-notes.md
- 20:16:50|Program.cs|docs/architecture.md,docs/implementation-notes.md


## [20:44] turn

**최근 응답 발췌:**

> ## 답부터 — 커서 모양 변경은 매력적이지만 **막다른 길**입니다  20개 대안을 발굴해 Win32 API 실존 여부까지 검증한 결과, **커서 자체를 건드리는 안은 4개 전부 REJECT** 되었고 위험도가 8~9/10 으로 최고였습니다. 이유가 두 가지인데, 첫 번째가 결정적입니다.  ### ① 타이핑 중에는 마우스 포인터가 **숨겨집니다**  검증 에이전트가 이 PC 에서 실측했습니다:  ''' SystemParametersInfo(SPI_GETMOUSEVANISH) = 1  (Windows 기본값 ON) '''  Windows 는 **키보드를 치기 시작하면 마우스 포인터를 감춥니다.** 그런데 한/영을 잘못 알아 문장을 통째로 날리는 사고는 **바로 타이핑하는 도중**에 일어납니다. 즉 커서를 아…

**커밋되지 않은 변경:**
```
 M docs/sessions/2026-07-22.md
```


## [20:59] turn

**최근 응답 발췌:**

> 세 가지 모두 확인했습니다. 1번은 제 설명이 부정확해서 오해를 만들었네요 — 바로잡겠습니다.  ## 1. 마우스 포인터와 캐럿은 **서로 다른 물건**입니다  '''    메모장    ┌──────────────────────────────────┐    │  안녕하세요▏                     │ ← 캐럿 (깜빡이는 세로 막대)    │                                  │   키보드가 만든다. 글자가 찍히는 자리    │                    ⌶             │ ← 마우스 포인터 (I-beam 모양)    │                                  │   마우스가 만든다. 마우스가 있는 자리    └──────────────…

**커밋되지 않은 변경:**
```
 M docs/sessions/2026-07-22.md
```


## [2026-07-22 21:09] session-end (other)

**자동 wip 커밋 생성됨** — 이 마무리 블록까지 포함합니다. 다음 세션 시작 시 이어 작업합니다.

---

## [21:07] 세션 정리 2부 — 진단 로그 강화 + IME 표시 방식 대안 탐색(PR-27)

### 했음

**1. 진단 로그에 창 식별자 추가 (커밋 `c359171`)**
- `PositionUpdated` 디버그 로그에 `hwnd=0x…`·`class=…` 추가. 한 프로세스가 top-level 창을 여러 개 쓰는 앱(파일 관리자 + 내장 뷰어)에서 `process=` 만으로는 인디가 어느 창에 붙었는지 구분 불가 — PR-26 조사에서 토탈 커맨더 위치 진동의 원인 규명이 막힌 지점.
- `Core/Logging/Logger.cs` 에 **`IsEnabled(LogLevel)` 신설**. 보간 문자열 합성이 호출 사이트 책임(`ILogSink` 계약)이라 `GetClassName`(P/Invoke) 조회를 레벨 가드로 감쌌다. **같은 가드를 `TryHandleFilter` 의 `Filter HIDE deferred` 에도 적용** — 감지 스레드 핫패스(80ms 폴링)에서 레벨과 무관하게 매번 클래스명을 조회하던 기존 비용 제거(부수 성능 개선).
- 검증: debug 0/0 + AOT publish 경고 0 + **90/90 PASS** + P1–P6 invariant 위반 0(`DllImport` 소스 0매치). docs 3곳 동기화(architecture Logger surface / implementation-notes 진단 필드 / CHANGELOG `[Unreleased]`).
- **배포**: 앱이 꺼져 있던 틈에 `D:\_portable\KoEnVue\KoEnVue.exe` 를 새 빌드로 교체(5,034,496 B, SHA256 `B84DBCFD…`). 기존 exe 는 스크래치패드 백업 + `v0.9.9.6` 태그/GitHub Release 로 복구 가능. 버전 bump 없음(릴리즈가 아니라 진단 빌드).

**2. IME 상태 표시 방식 대안 탐색 → [PR-27](../improvement-plan/PR-27-ime-indication-alternatives.md) 박제 (코드 변경 0)**
- 사용자 질문: "감지는 정확한데, 알아낸 한/영 상태를 **어떻게 알릴 것인가**. 지금의 플로팅 라벨/커서 주위 그래픽 말고 더 나은 방법? 예: 커서 모양 자체 변경".
- 워크플로우 `ime-indication-alternatives`(즉석) — 5렌즈 병렬 발굴 → **대안별 Win32 API 실존을 공식 문서로 검증** → 가치·거슬림·오염위험 심사. **44 에이전트 / 실패 0 / 발굴 20건 → 기술적 탈락 1 → 심사 19**.
- **커서 자체 교체 4건 전부 REJECT(위험 8~9/10)**. 사유 ① 실측 `SPI_GETMOUSEVANISH=1` — **타이핑을 시작하면 마우스 포인터가 숨겨진다**. 오입력 사고는 타이핑 도중 일어나므로 신호가 가장 필요한 순간에 사라짐(코드로 해결 불가). ② `SetSystemCursor` 는 세션 전역이라 **강제 종료 시 로그오프까지 원복 불가**, 복원용 `SPI_SETCURSORS` 자체가 접근성 커서 크기를 리셋하는 알려진 버그. 현행 두 방식은 프로세스와 함께 사라짐 — 질적 차이.
- **1위 = 캐럿 트윈**(7/10, PROTOTYPE): 마우스 포인터가 아니라 **캐럿**에 밀착한 색 막대. 가치 8(시선 지점 표시는 현재 미충족 영역 — 라벨은 창 모서리, 커서 인디는 타이핑 중 시선 밖) / **위험 2**(전역 상태 0, 강제 종료돼도 커널이 정리). `LayeredCursorBase` 재사용률이 높아 1단계 비용이 작다. **치명적 함정**: 캐럿 미검출 앱에서 "막대 없음"을 사용자가 "영문"으로 오독 → 침묵 실패가 거짓 신호. 한/영 양쪽 모두 착색 + 미검출 시 라벨 자동 폴백이 타협 불가 전제.

**3. 후속 질문 3건 확인**
- **용어 정정**: 마우스 포인터(텍스트 위에서 I-beam 모양)와 캐럿(깜빡이는 세로 막대)은 **별개이며 동시에 다른 자리에 존재**. "타이핑 중 숨겨진다"는 I-beam 포인터가 사라진다는 뜻이지 캐럿이 되는 게 아니다.
- **캐럿 커버리지 구멍 실재 확인** — Terminal(`microsoft/terminal#6805`), riched20 선택이동(MS 문서 명시), 계산기 워크시트, Chromium(미검증), **관리자 권한 창(UIPI)**. 마지막은 dev-notes 2026-05-27 확정 결론(asInvoker + admin cmd → `SendMessageTimeoutW` ABORT)과 **같은 뿌리** — 그 앱에선 지금도 감지 자체가 안 되므로 캐럿 트윈이 새로 잃는 건 없다. 반면 **Terminal 은 IME 는 잡히고 캐럿만 못 잡아** 진짜 구멍.
- **커서 인디 부드러움**: 이미 **16ms(60fps)**, 실측 모니터 둘 다 **60Hz** → 폴링은 병목 아님. 더 나은 추적(`WH_MOUSE_LL`/`WM_INPUT`)은 dev-notes 2026-05-27 에서 이미 검토·거부됨. 진짜 병목은 (a) 타이머 해상도 흔들림(~15.6ms 틱) (b) **DWM 합성 지연 — 하드웨어 커서 플레인 대비 구조적으로 따라잡기 불가**. 유일한 실질 개선은 예측 외삽(방향 전환 시 오버슈트 부작용). **증상이 "끊김"인지 "뒤처짐"인지 판별이 선행돼야 함**(해법이 정반대).
- **문서 불일치 발견(미수정)**: CHANGELOG 기능 목록의 "캐럿 이동 감지" 항목이 실제 코드와 불일치 — `GetCaretPos`/`rcCaret` 사용 0건.

### 왜
- 진단 로그: 사용자가 명시 요청. PR-26 의 토탈 커맨더 진동이 "정상적인 창 전환인지 결함인지" 구분 불가한 상태로 막혀 있었고, 그 해소가 한 줄 로그 개선으로 가능했다.
- 표시 대안: 사용자가 직접 "커서 모양 변경"을 제안 — **왜 안 되는지 근거를 남기지 않으면 같은 아이디어가 반복 재검토**된다(재조사 비용 44 에이전트/3.35M 토큰). API 를 지어내지 않도록 발굴과 기술검증을 별도 단계로 분리한 것이 핵심 설계.
- 사용자 지시대로 **결함 수정은 미착수** — PR-26/27 모두 기록만.

### 다음
- **PR-26 수정 여부·방향 결정**(미결). 착수 시 planner 설계 → 구현 → verifier → reviewer → 실기 smoke.
- **토탈 커맨더 진동 재현** — 새 exe 의 `hwnd`/`class` 로그로 진동하는 두 창을 즉시 특정 가능. `log_level=DEBUG` 로 올리고 재현하면 됨.
- **캐럿 트윈 1단계 프로토타입**(선택) — `GetGUIThreadInfo`+`ClientToScreen`+`LayeredCursorBase` 사각 렌더러만. 판정 기준은 오직 "캐럿이 색을 바꿨다로 읽히는가".
- 커서 부드러움은 증상 판별 후.
- CHANGELOG "캐럿 이동 감지" 문구 사실 확인 후 정정.

### 커밋
- `c359171` feat(diag): PositionUpdated 로그에 hwnd/class 추가 + Logger.IsEnabled 레벨 가드
- (이번 문서 커밋) PR-27 박제 + 세션 정리 2부

---
