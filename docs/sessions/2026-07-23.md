
## [2026-07-23 08:34] session-end (other)

**자동 wip 커밋 생성됨** — 이 마무리 블록까지 포함합니다. 다음 세션 시작 시 이어 작업합니다.

---


## [16:31] 세션 정리 — SetTimer 15ms 적용 + PR-29 이동 중 안개 distraction↓

### 했음

**1. `/resume-session` → SetTimer 16→15 적용 (커밋 `700ab8a`)**
- 전일 PR-28 실측: `SetTimer(16)` ≈30.78ms(~32fps), `SetTimer(15)` ≈15.56ms. 대기 중이던 적용 결정 후 `CursorAlwaysPollMs`·`AnimationFrameMs` 디폴트·주석 정정.
- 사용자 체감: **"별 차이 없음·머뭇거림"**. 뒤처짐 수용(옵션1) — 예측 외삽은 오버슈트 부작용으로 **보류**. DWM 1프레임 + 하드웨어 커서 플레인 대비 구조적 한계는 남음.

**2. PR-29 초안→구현 — 이동 중 distraction 감소 (미커밋 dirty)**
- 동기: 폴링 개선만으로는 체감 부족 → 이동 중 시각 자극을 줄여 "한 박자 늦음"을 덜 의식하게.
- 수렴 경로: soft 헤일로 → 원별 두께/알파 → **3원 공통 가우시안 안개**.
- 최종 파라미터: soft 시 하드코어 0, σ≈halo×14, `MotionFogPad` 28, alpha≈0.22 균일. CAPS ON=3원 / OFF=2원 유지.
- 신규 `App/UI/CursorMotionDim.cs` + 단위테스트. Settings·트레이 UI, `_displayAlpha` 정리. docs: `PR-29-cursor-motion-distraction-reduction.md`, INDEX, config-reference, implementation-notes, CHANGELOG.
- 사용자: **"이 정도로 만족"** → wrap-up.

### 왜
- 16→15는 저비용·가역(숫자 2개)이라 증상 판별용으로 먼저 적용. 체감 미미 → 외삽으로 가지 않고 지각 마스킹(안개) 축으로 전환.
- 안개는 원 윤곽/센트로이드를 흐려 위치 오차의 가독성을 떨어뜨림(PR-28이 기각한 "순수 알파/대칭 블러"와 다른 경로 — 이동 전용 soft 렌더).
- CAPS 원 개수 시그널은 유지 — 한/영 판독과 이동 distraction↓를 분리.

### 다음
1. **PR-29 dirty 커밋/푸시** — wrap-up에서 사용자 확정 후. 대상: `CursorMotionDim.cs`, Overlay/Renderer/LayeredCursorBase/CursorStyle, Settings·Tray·I18n·AppConfig·DefaultConfig, tests, PR-29/INDEX/config-reference/implementation-notes/CHANGELOG.
2. Tier-3 실사용 지속 관찰 — 안개 농도(α·σ) 미세조정 가능.
3. **이월**: PR-26 UserHidden 해제, 토탈커맨더 진동(`hwnd`/`class` 로그로 재현), 캐럿 트윈(PR-27), 예측 외삽(뒤처짐 재논의 시).

### 커밋 (HEAD 기준)
- `700ab8a` fix(cursor): SetTimer 양자화에 맞춰 AlwaysPoll/AnimationFrame 디폴트 16→15
- `main` ahead 1 (`700ab8a` 미푸시 가능) · **PR-29 전부 uncommitted dirty**

---

## [17:36] 세션 정리 — 좌클릭 일시 숨김·정중앙 폴백·cleanup UX

### 했음
- `03f451a` 메인 인디 짧은 좌클릭 → `_clickDismissed` 일시 숨김(클릭 통과). 포커스 변경 또는 한/영(IME) 변경 시 재표시. 드래그는 HTCLIENT+SetCapture → SM_CX/CYDRAG 임계 후 HTCAPTION 승격.
- `ef28305` 고정 모드 `default_indicator_position == null` 폴백을 우상단 오프셋 → `ResolveWorkAreaCenter`. `DefaultIndicatorOffsetX/Y` 제거.
- `dbcf3b6` 위치 기록 정리 UX: 모드 태그 `(고정)/(창)/(고정·창)` + 실행 중, 안내 문구(고정·창 기록 모두 표시·삭제 시 두 모드 초기화). `PositionCleanupServiceTests` 추가. 삭제 로직은 원래부터 모드 무관 합집합 — 동작 변경 없음, UX만.
- 빌드/테스트: 커밋 경로에서 통과. working tree clean, `main == origin/main`.
- 수동 smoke(좌클릭 숨김·정중앙·cleanup 태그) 사용자 확인 완료.

### 왜
- 좌클릭 숨김은 **UserHidden(트레이 영속)과 분리** — 일시(`_clickDismissed`)만. 재표시 트리거 = 포커스|IME (작업 전환·입력 모드 전환 시 다시 보이면 됨).
- 고정 모드 기본 위치는 모니터마다 우상단 오프셋이 어색 → **작업 영역 정중앙**이 중립 폴백.
- cleanup은 이미 모드 무관 합집합 삭제였음 → 사용자 혼란만 태그·문구로 해소 (추가 동작 변경 없음).

### 다음
1. **PR-26** UserHidden 해제 시 필터 미재평가 — 보류(미착수).
2. (선택) Tier-3 실사용에서 좌클릭 dismiss / 드래그 임계 체감 관찰.
3. 이월 유지: 토탈커맨더 진동, 캐럿 트윈(PR-27), 예측 외삽.

### 커밋 (HEAD 기준 3개)
- `dbcf3b6` feat(cleanup): 위치 기록 정리에 모드 태그·안내 문구 보강
- `ef28305` feat(overlay): 고정 모드 기본 위치를 모니터 작업 영역 정중앙으로
- `03f451a` feat(overlay): 메인 인디 좌클릭 일시 숨김, 드래그는 위치 이동

---
