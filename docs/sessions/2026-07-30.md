
## [08:49] turn

**최근 응답 발췌:**

> 릴리즈 보류 결정을 반영해 정리를 마쳤습니다.  **`f7031d8`** — [AUDIT-2026-07-30-concurrency-bug-hunt.md](docs/improvement-plan/AUDIT-2026-07-30-concurrency-bug-hunt.md) 신설 + INDEX 갱신 (push 완료)  ## 67건의 실체는 14그룹이었습니다  워크플로우의 중복 제거 키가 `파일 + kind + desc 앞 60자` 라서, **같은 결함을 라운드·finder 마다 다르게 서술하면 별건으로 집계**됩니다. 병합해 보니 이렇습니다.  | 우선순위 | 그룹 | 원본 건수 | |---|---|---| | **P0** | §A 모달 다이얼로그 재진입 → 프로세스 종료 | 7건이 1그룹 | | **P1** |…

## [08:55] 세션 정리 — v1.0.0.0 배포 전 검증 → 결함 수정 → 릴리즈 보류

> **2026-07-29 저녁 세션의 연속**(자정 경과). 전반부는 [2026-07-29.md](2026-07-29.md) — 그 파일의 `[2026-07-29 20:32] session-end` 블록 **이후에도 계속**된 구간이 아래다.

### 했음

요청: "v1.0.0.0 정식 배포하고 싶은데 그 전에 문제 없는지 확인". 3층 검증안 제시 → 사용자가 **"최대 — release-review + bug-hunt"** 선택(버전 형식 `v1.0.0.0` 4-part 확인).

**1층 기계 게이트 — 전부 통과.** debug 경고0/오류0 · 테스트 165 · AOT publish 4.91 MiB · `docs/conventions.md:61` P1–P6 invariant 23개 전부 기대값 일치 · Actions 5회 녹색 · 런타임 로그 ERROR/WARN 0.
- publish 폴더의 `koenvue_crash.txt` = 크래시 아님. `AdminElevation` 이 로거 초기화 전이라 crash 파일을 공용 채널로 재사용 + 태그(`ELEVATION` vs `FATAL`) 구분하는 문서화된 설계(결정 #5).
- `UpdateCheckerTests` 에 `0.9.9.7 → 1.0.0.0` 케이스 없음 — major 승격으로 자릿수 줄어드는 구간 = 이번 릴리즈 알림이 정확히 타는 경로.

**2층 `/release-review` 3회 — 1·2차 공회전.** 기본 SCOPE 가 `HEAD~1..HEAD` 이고 직전 커밋이 세션 로그 문서 → correctness·보안·동시성 3차원이 마크다운만 받고 "볼 게 없다" 반환(invariant 차원만 넓게 봐 위반 검출). `Workflow({name,args:{scope}})` 가 **2회 연속 스크립트에 도달하지 않음**(결과 JSON `scope` 필드가 기본 문구 그대로) → 3차는 **스크립트의 SCOPE 상수를 직접 고쳐 `Workflow({scriptPath})`** 재실행. 범위 `v0.9.9.7..HEAD` / 31 에이전트 / 확정 10건. 동작 결함은 이때만 나왔다.

**확정 10건 전부 수정 — `93d0360`**
- `App/Program.SystemEvents.cs` — 테마·강조색 변경 시 `Tray.UpdateState` 미호출 → 셸이 이전 HICON 보유해 **트레이 아이콘만 옛 색으로 잔존**. 선재이나 이번 아이콘 재디자인으로 Fg 면적↑ 체감 증가.
- `App/Program.Timers.cs` — `EnableCursorOverlay` GDI 초기화 예외가 `WndProc` 최상위 catch 까지 올라가, `Settings.Save` 를 끝낸 좌클릭 경로에서 **config 는 저장됐는데 UI 전이가 통째로 스킵** → 영구 불일치. try/catch 흡수 + 창 되돌림.
- `App/UI/Tray.cs` — 아이콘 생성 실패 시 NULL HICON 을 셸에 넘겨 **유효한 이전 아이콘까지 교체**, 빈 트레이 고착(NIM_ADD 재시도가 같은 NULL 최대 30회 재제출). 직전 유효 아이콘 유지 + 툴팁만 갱신.
- 규약 4건(동작 불변): `BytesPerPixel` 재선언 → `DibSectionFactory.BytesPerPixel`(감사 묶음1 이 `CursorRenderer` 에서 없앤 패턴 회귀) · COLORREF 채널 분해 인라인 → `ColorHelper.ColorRefToRgb` · 알파 리터럴 → `byte.MaxValue`(`TrayIcon.PaintIcon`, `OverlayAnimator`). 테스트 165 → **167**(1.0 승격 2건).
- 문서 6곳: `release-procedure.md` git tag 예시가 `v0.9.3.0.0`(5-part)인데 주석은 "(4-part)" → 정정(따랐으면 `v1.0.0.0.0` 오태깅) + 사전조건 테스트 수 90→165 · `conventions.md`/`architecture.md` baseline 142/16 → 165/17 + `TrayLeftClickToggleTests` 등재(PR-33 sync 잔여 해소) · `implementation-notes.md` 테마 핸들러 호출 순서·트레이 갱신 근거 · **`harness.md` 에 Workflow args 미전달 함정**(결과 scope 필드 확인 + scriptPath 재실행) · `reviewer.md` grep 보강.

**3층 실기** — 첫 실행·아이콘·좌클릭 4단계·관리자 권한·다중 모니터 체크리스트 → 사용자 "모두 정상".

**`/bug-hunt` 5라운드** — 296 에이전트 / 25.8M 토큰 / 6211초 / 확정 67건 → **고유 14그룹**(dedup 키가 `파일+kind+desc앞60자` 라 서술만 달라도 별건 집계).
- **§A (P0, 메인 직접 재현)** 모달 3종(`CleanupDialog`/`SettingsDialog`/`ScaleInputDialog`)이 재진입 가드보다 **먼저** 공유 static 을 파괴 → NRE → `[UnmanagedCallersOnly]` DlgProc 에 try/catch 없어 **NativeAOT 프로세스 종료**. 사슬: `DialogShell.cs:101` 가드가 Run 내부 · `CleanupDialog.cs:71/116` 프롤로그·에필로그가 가드 밖(`_items = null!`) · `Tray.Menu.cs:22` ShowMenu 무가드 · `ModalDialogLoop.cs:68` 필터 없는 `GetMessageW` · `Program.cs:427` WM_TRAY_CALLBACK 무게이트. `EnableWindow(owner,false)` 는 마우스만 막고 explorer 의 posted message 는 못 막음. 재현: 다이얼로그 열어둔 채 트레이 우클릭 → 같은 항목 → 원래 창에서 확인.
- **P1** §C Logger 좀비 drain 부활 + `_fileWriter` 동시 Dispose(트리거=로그 설정 편집) · §G config 파싱 실패 시 전 필드 디폴트를 디스크에 저장 → 설정 전멸 · §B 모달 중 `WM_CONFIG_CHANGED` 재진입 → 다이얼로그 스냅샷이 핫리로드 되돌림.
- **P2/P3** 프로필 LRU stale·desync, 캐시 무효화 순서, mtime self-bump TOCTOU, `tray_enabled` 전이 미처리, `_hwndPositions` HWND 키 누수, 종료 `Join(500)`, `_vdmFailCount` 비원자 등(전체는 AUDIT 문서).
- **커버리지 한계** — 토큰 한도로 에이전트 30개 실패, **라운드 4·5 의 finder 4종 전멸** → 그 두 라운드는 탐색 자체가 없었는데 워크플로우가 "새 결함 0"으로 읽어 dry 카운터를 올리고 종료(수렴이 아니라 **자원 소진**). 검증 22건 실패로 2표 미달 탈락한 실재 결함 가능 → **하한선**.
- 14그룹 **전부 선재** — 릴리즈 후보(`v0.9.9.7..HEAD`)가 만든 건 0.

**사용자 결정: v1.0.0.0 보류** — 제시 4안 중 "릴리즈 보류, 67건 정리 우선" → `docs/improvement-plan/AUDIT-2026-07-30-concurrency-bug-hunt.md` 신설(14그룹·우선순위·67건 매핑·커버리지 한계·**검증 신뢰도 2등급**[메인 직접 재현 vs 2/3 투표·미재확인]·처리 계획 묶음 1~7) + `improvement-plan/INDEX.md` 갱신 → `f7031d8`. 버전 `0.9.9.7` 유지, CHANGELOG `[Unreleased]` 에 수정분 누적.

### 왜
- **"최대" 검증이 값을 했다** — 기계 게이트 invariant 23개가 전부 통과한 상태에서 P3/P4 위반 3건이 리뷰에서 나왔다. 중복 구현·매직넘버는 grep 이 잡는 종류가 아니고 `conventions.md:57` 이 그 한계를 자인.
- **스코프 확인 없는 "확정 0건"은 신뢰 금지** — 1·2차는 빌드 게이트 통과 + 결과 형식 정상이라 겉보기엔 완료였으나 코드를 안 봤다.
- **보류 사유는 "이번 변경이 위험해서"가 아니다** — 14그룹 전부 선재이고 현재 main 은 v0.9.9.7 보다 개선된 상태. 1.0 정식이라는 이름에 맞추려면 사용자 조작으로 죽는 §A 를 먼저 닫아야 한다는 판단.
- 67이란 수에 겁먹지 말 것 — 실체 14그룹, P0 는 1그룹(수정은 가드 위치 이동 + 한 줄 추가 수준).

### 다음
1. **묶음 1(§A) = v1.0.0.0 재개 조건** — 다이얼로그 3종 `Show()` 맨 앞으로 가드 이동 + `Tray.ShowMenu` 에 `ModalDialogLoop.IsActive` 가드(이중 방어). 회귀 테스트: `IsActive` 참일 때 static 손대지 않고 조기 반환 — 순수 판정부 분리 필요.
2. 묶음 2(§G config 로드 실패 격리 + §C Logger 세대 토큰·lock) → 묶음 3(§B 모달 중 config 이벤트 보류/필드 병합 커밋 + §F mtime 원자화) → 묶음 4~6(캐시 원자성·무효화 순서 / 수명·전이 누락 / 오기 주석).
3. **새 세션에서 `/bug-hunt` 재개** — 라운드 4·5 미탐색 보완.
4. 묶음 1 완료 후 v1.0.0.0 재판단 — csproj `1.0.0.0`·태그 `v1.0.0.0`·CHANGELOG `[1.0.0.0]` 3곳, 재-publish 산출물의 exe + sha256 **쌍을 그대로** 첨부(NativeAOT 비결정성).

### 커밋 (HEAD 기준 2개, 둘 다 push 완료)
- `f7031d8` docs(audit): bug-hunt 67건 → 고유 14그룹 정리 + v1.0.0.0 보류 근거
- `93d0360` fix: 릴리즈 리뷰 확정 결함 수정 — 트레이 아이콘 테마 추종 + 실패 경로 견고성 + P3/P4 규약

---
